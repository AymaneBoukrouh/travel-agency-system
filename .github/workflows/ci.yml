name: Run API tests

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'web/**'

  pull_request:
    branches:
      - 'README.md'
      - 'web/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
        
    - name: Run Tests
      run: |
        chmod +x ./api/test.sh
        ./api/test.sh
